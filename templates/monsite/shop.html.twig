{% extends 'base.html.twig' %}

{% block body %}
<div class="row">
	<div class="col-12 col-lg-6">
		<img src="{{ asset('' ~ produit.image ~ '') }}" alt="mon super produit" class="imgshop" />
	</div>
	<div class="col-12 col-lg-6 mt-5 shop">
		<div class="divshop">
			<h2>
				{{ produit.nom }}
			</h2>
		</div>
		<div class="mt-3 mb-3 mr-5 text-dark">
			<p>
				{{ produit.description}}
			</p>
		</div>
		<div class="text-dark">
			<h4>
				{{ produit.price }} €
			</h4>
		</div>
		<div>
			<a class="btn btn-danger mt-3" href="{{ path('monpanier_add', {'id': produit.id}) }}" role="button">
			<i class="fas fa-shopping-cart mr-1"> Acheter</a>
		</div>
	</div>	
</div>
{% endblock %}